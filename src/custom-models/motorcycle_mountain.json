{
  "distance_influence": 55.0,
  "speed": [
    { "if": "true", "multiply_by": "0.92 * car_average_speed" },
    { "if": "true", "limit_to": 100 },
    { "if": "smoothness == INTERMEDIATE", "limit_to": 70 },
    { "if": "smoothness == BAD || smoothness == VERY_BAD", "limit_to": 45 }
  ],
  "priority": [
    { "if": "!car_access", "multiply_by": 0 },
    { "if": "road_access == PRIVATE", "multiply_by": 0 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0.01 },
    { "if": "road_class == PRIMARY", "multiply_by": 0.7 },
    { "if": "road_class == SECONDARY", "multiply_by": 1.1 },
    { "if": "road_class == TERTIARY || road_class == UNCLASSIFIED", "multiply_by": 1.25 },
    { "if": "smoothness == GOOD || smoothness == EXCELLENT", "multiply_by": 1.08 },
    { "if": "surface == GRAVEL || surface == GROUND || surface == DIRT || surface == SAND || surface == GRASS", "multiply_by": 0.6 }
  ]
}
