{
  "distance_influence": 25.0,
  "speed": [
    { "if": "true", "multiply_by": "0.85 * car_average_speed" },
    { "if": "true", "limit_to": 95 },
    { "if": "smoothness == BAD || smoothness == VERY_BAD", "limit_to": 40 },
    { "if": "surface == GRAVEL || surface == GROUND || surface == DIRT || surface == SAND || surface == UNPAVED || surface == GRASS", "limit_to": 32 }
  ],
  "priority": [
    { "if": "!car_access", "multiply_by": 0 },
    { "if": "road_access == PRIVATE", "multiply_by": 0 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0.001 },
    { "if": "road_class == PRIMARY", "multiply_by": 0.45 },
    { "if": "road_class == SECONDARY", "multiply_by": 0.85 },
    { "if": "road_class == TERTIARY || road_class == UNCLASSIFIED", "multiply_by": 1.45 },
    { "if": "roundabout", "multiply_by": 0.35 },
    { "if": "surface == ASPHALT || surface == PAVED", "multiply_by": 1.12 },
    { "if": "smoothness == BAD || smoothness == VERY_BAD", "multiply_by": 0.75 },
    { "if": "surface == GRAVEL || surface == GROUND || surface == DIRT || surface == SAND || surface == UNPAVED || surface == GRASS", "multiply_by": 0.55 }
  ]
}
