{
  "distance_influence": 75.0,
  "speed": [
    { "if": "true", "multiply_by": "0.98 * car_average_speed" },
    { "if": "true", "limit_to": 120 },
    { "if": "smoothness == BAD || smoothness == VERY_BAD", "limit_to": 50 },
    { "if": "surface == GRAVEL || surface == GROUND || surface == DIRT || surface == SAND || surface == UNPAVED || surface == GRASS", "limit_to": 40 }
  ],
  "priority": [
    { "if": "!car_access", "multiply_by": 0 },
    { "if": "road_access == PRIVATE", "multiply_by": 0 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0.02 },
    { "if": "road_class == PRIMARY || road_class == SECONDARY", "multiply_by": 1.15 },
    { "if": "road_class == TERTIARY || road_class == UNCLASSIFIED", "multiply_by": 0.95 },
    { "if": "surface == ASPHALT || surface == PAVED", "multiply_by": 1.08 }
  ]
}
