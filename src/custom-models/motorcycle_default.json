{
  "distance_influence": 60.0,
  "speed": [
    { "if": "true", "multiply_by": "0.94 * car_average_speed" },
    { "if": "true", "limit_to": 115 },
    { "if": "smoothness == EXCELLENT || smoothness == GOOD", "limit_to": 105 },
    { "if": "smoothness == INTERMEDIATE", "limit_to": 78 },
    { "if": "smoothness == BAD", "limit_to": 50 },
    { "if": "smoothness == VERY_BAD", "limit_to": 32 },
    { "if": "surface == COBBLESTONE || surface == GRAVEL || surface == GROUND || surface == DIRT || surface == SAND || surface == UNPAVED", "limit_to": 42 },
    { "if": "surface == GRASS", "limit_to": 30 }
  ],
  "priority": [
    { "if": "!car_access", "multiply_by": 0 },
    { "if": "road_access == PRIVATE", "multiply_by": 0 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0.03 },
    { "if": "road_class == PRIMARY", "multiply_by": 1.0 },
    { "if": "road_class == SECONDARY", "multiply_by": 1.08 },
    { "if": "road_class == TERTIARY || road_class == UNCLASSIFIED", "multiply_by": 1.12 },
    { "if": "roundabout", "multiply_by": 0.8 },
    { "if": "surface == ASPHALT || surface == PAVED", "multiply_by": 1.08 },
    { "if": "smoothness == BAD || smoothness == VERY_BAD", "multiply_by": 0.68 },
    { "if": "surface == GRAVEL || surface == GROUND || surface == DIRT || surface == SAND || surface == UNPAVED || surface == GRASS", "multiply_by": 0.65 }
  ]
}
