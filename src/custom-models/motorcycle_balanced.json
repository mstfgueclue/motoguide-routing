{
  "distance_influence": 65.0,
  "speed": [
    { "if": "true", "multiply_by": "0.95 * car_average_speed" },
    { "if": "true", "limit_to": 120 },
    { "if": "smoothness == EXCELLENT || smoothness == GOOD", "limit_to": 110 },
    { "if": "smoothness == INTERMEDIATE", "limit_to": 80 },
    { "if": "smoothness == BAD", "limit_to": 55 },
    { "if": "smoothness == VERY_BAD", "limit_to": 35 },
    { "if": "surface == COBBLESTONE || surface == GRAVEL || surface == GROUND || surface == DIRT || surface == SAND || surface == UNPAVED", "limit_to": 45 },
    { "if": "surface == GRASS", "limit_to": 30 }
  ],
  "priority": [
    { "if": "!car_access", "multiply_by": 0 },
    { "if": "road_access == PRIVATE", "multiply_by": 0 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0.01 },
    { "if": "surface == ASPHALT || surface == PAVED", "multiply_by": 1.1 },
    { "if": "smoothness == BAD || smoothness == VERY_BAD", "multiply_by": 0.7 }
  ]
}
